{
  "name": "pgormjs",
  "version": "0.0.0",
  "description": "PostgreSQL-only, type-safe TypeScript ORM. Zero dependencies. Pre-alpha — not yet usable.",
  "license": "MPL-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run test/schema test/sql test/compile",
    "test:live": "vitest run test/driver test/codec test/fuzz",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/schema/tsconfig.json && tsc --noEmit -p test/driver/tsconfig.json && tsc --noEmit -p test/codec/tsconfig.json && tsc --noEmit -p test/sql-compile.tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pgormjs/pgorm.git"
  },
  "keywords": [
    "postgres",
    "postgresql",
    "orm",
    "typescript",
    "migrations",
    "typesafe"
  ]
}
