{
  "name": "@pgorm/kit",
  "version": "0.0.0",
  "description": "Migration engine and CLI for pgorm: catalog diffing, plan generation, lint, verify. Pre-alpha — not yet usable.",
  "license": "MPL-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pgormjs/pgorm.git"
  },
  "devDependencies": {
    "@supabase/pg-delta": "1.0.0-alpha.39",
    "@types/pg": "^8.21.0",
    "pg": "^8.23.0",
    "vitest": "^4.1.10"
  }
}
