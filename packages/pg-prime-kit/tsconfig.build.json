{
  "//": "The publish emit (design/08 §3.1, §3.2). `isolatedDeclarations: true` here, per design §3.2 — the kit's API is ordinary functions and data, so it is free, it buys parallel emit, and it guarantees no inferred type leaks a deep internal path into the published `.d.ts`. It cost exactly ONE annotation to turn on (`MIGRATION_NAME: RegExp` in src/plan/plan.ts). There is no esbuild bundle: design §3.1 bundles the kit because of CLI cold-start, and the CLI does not exist yet — see the AS BUILT note under §3.1.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "isolatedDeclarations": true,
    "noEmit": false
  },
  "include": ["src"]
}
