{
  "name": "@pg-prime/kit",
  "version": "0.0.0",
  "description": "Migration engine for pg-prime: catalog diffing, plan generation, prove-on-shadow-clone, apply. Pre-alpha — `pg-prime migrate apply|status|baseline|unlock` plus the programmatic API.",
  "license": "MPL-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "bin": {
    "pg-prime": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types@<5.9": "./dist/unsupported-typescript.d.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "node ../../tools/build-package.mjs",
    "clean": "node ../../tools/build-package.mjs --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json",
    "test:pg": "vitest run"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pg-prime/pg-prime.git"
  },
  "dependencies": {
    "@types/pg": "^8.21.0",
    "pg": "^8.23.0"
  },
  "peerDependencies": {
    "pg-prime": ">=0.0.0"
  },
  "devDependencies": {
    "pg-prime": "workspace:*",
    "vitest": "^4.1.10"
  }
}
